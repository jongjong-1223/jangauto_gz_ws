# CAL 상태 진입 시 mission_state_machine.py가 호출하는 캘리브레이션 액션.
# 무엇을 캘리브레이션하는지(IMU 바이어스/GPS 기준점 등)는 아직 미정 —
# 지금은 goal에 필드 없이 "캘리브레이션 시작"만 의미한다.

# goal (필드 없음 — 실제 대상이 정해지면 추가)
---
# result
bool success
string message
---
# feedback
string status
